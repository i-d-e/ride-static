// tooltip.js — Cross-Reference preview tooltip (stub).
//
// interface.md §11: hovering an inline cross-reference (Fig. N, footnote
// anchor, K-criterion) shows a small preview. The preview content (image
// thumbnail, footnote body, criterion title) is a Phase 7 concern — the
// ref-resolver will surface the necessary data attributes on the anchor.
//
// This stub exists so the script tag in base.html does not 404. Once
// Phase 7 lands, the tooltip wiring goes here.
export {};
